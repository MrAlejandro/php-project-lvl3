version: "3.7"

services:
  app:
    build: .
    volumes:
      - .:/app:cached
    ports:
      - "8000:8000"
    command: 'php artisan serve --host 0.0.0.0 --port 8000'
    stdin_open: true
    tty: true
